# PostgreSQL (run locally, no Docker)
# Create DB and user first, e.g.:
#   createdb gtm_db
#   createuser -P gtm_user
#   psql -c "GRANT ALL ON DATABASE gtm_db TO gtm_user;"
DATABASE_URL="postgresql://gtm_user:YOUR_PASSWORD@localhost:5432/gtm_db?schema=public"
