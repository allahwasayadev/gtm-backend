# PostgreSQL (run locally, no Docker)
# Create DB and user first, e.g.:
#   createdb ovrlap_db
#   createuser -P ovrlap_user
#   psql -c "GRANT ALL ON DATABASE ovrlap_db TO ovrlap_user;"
DATABASE_URL="postgresql://ovrlap_user:YOUR_PASSWORD@localhost:5432/ovrlap_db?schema=public"
